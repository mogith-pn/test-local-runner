# Config file for the llama-cpp-runner

model:
  id: "lcpp-llama-3-2-1b-instruct"
  user_id: "mogith-p-n"
  app_id: "test-co"
  model_type_id: "text-to-text"

build_info:
  python_version: "3.11"

inference_compute_info:
  cpu_limit: "3"
  cpu_memory: "12Gi"
  num_accelerators: 1
  accelerator_type: ["NVIDIA-A10G"]
  accelerator_memory: "24Gi"

